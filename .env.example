# Your timezone, https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=#Europe/London

# UNIX PUID and PGID, find with: id $USER
PUID=#1000
PGID=#1000

# The directory where app configuration and data will be stored.
APPS=#./apps
MEDIA=#./media

# --- Real Debrid ---
# API token from https://real-debrid.com/apitoken
REAL_DEBRID_API_KEY=#your-real-debrid-api-token

# --- TMDb (recommended) ---
# Free API key for accurate media naming. Get from https://www.themoviedb.org/settings/api
TMDB_API_KEY=#your-tmdb-api-key

# --- PocketBase admin account ---
# Email and password for the PocketBase superuser (created automatically on first boot)
EMAIL=#admin@example.com
PASSWORD=#your-pocketbase-password

# How often the organiser scans for new content (seconds, default: 300)
#SCAN_INTERVAL_SECS=#300

# --- Local access ---
# LAN IP of this machine. Auto-detected by setup-homepage.sh if omitted.
# Set manually if auto-detection picks the wrong interface (e.g. multiple NICs).
#LOCAL_IP=#192.168.1.100

# --- Remote access (Option B) ---
# Generate a reusable auth key at https://login.tailscale.com/admin/settings/keys
#TS_AUTHKEY=#tskey-auth-xxxx
# Tailscale API key — needed for automatic DNS record creation on container start.
# Generate at https://login.tailscale.com/admin/settings/keys (API access token)
#TS_API_KEY=#tskey-api-xxxx
# Your domain - services accessible at jellyfin.example.com, zurg.example.com, etc.
#DOMAIN=#example.com
# Cloudflare API token: https://dash.cloudflare.com/profile/api-tokens (Zone > DNS > Edit)
#CF_API_TOKEN=#your-cloudflare-api-token
# Cloudflare Zone ID: found on your domain's overview page in the Cloudflare dashboard (right sidebar)
#CF_ZONE_ID=#your-cloudflare-zone-id

# --- Homepage API keys ---
# Used by setup-homepage.sh to configure live dashboard widgets.
# Get these from each app's Settings/API page after initial setup.
#JELLYFIN_API_KEY=#your-jellyfin-api-key
#PORTAINER_API_KEY=#your-portainer-api-key